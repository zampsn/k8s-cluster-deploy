authentik:
  error_reporting:
    enabled: true

server:
  ingress:
    ingressClassName: nginx
    enabled: true
    hosts:
      - authentik.cluster.zachsampson.dev

postgresql:
  enabled: true

redis:
  enabled: true
